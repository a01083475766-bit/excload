node_modules
.next
.env
.env.local
.env.*.local
.env.smoke.local
.shipment-transmission-it.lock
.shipment-transmission-it-summary*.json

# Google Drive for Desktop 동기화 임시 파일 (커밋 금지)
.tmp.driveupload/
.tmp.driveupload*
logs
*.log

# 과거 빌드/비교용 (ZIP 해제본·백업 — 커밋하지 않음)
_onekl-no01_extracted/
onekl-no01.zip

# TypeScript incremental build info (커밋/풀리퀘 용도 아님)
tsconfig.tsbuildinfo

# -----------------------------------------------------------------------------
# docs/local/ — GitHub·원격 저장소에 포함하지 않음
# Cursor·AI가 작성한 검토·메모·대화 요약 등 “로컬 메모리 대용” 문서.
# 실행·빌드·배포에 필요한 것은 코드 쪽에 두고, 이 폴더는 개인/기기 로컬 기록용.
# -----------------------------------------------------------------------------
docs/local/

# -----------------------------------------------------------------------------
# 렌딩페이지/ — 로컬 랜딩 기획·이미지·초안 등 (GitHub·원격 저장소에 포함하지 않음)
# -----------------------------------------------------------------------------
렌딩페이지/

# -----------------------------------------------------------------------------
# API 신청용 서비스 소개서 (로컬 전용 — 사업자·담당자 개인정보 포함, 커밋 금지)
# 조사·문의 준비 자료라 GitHub·원격 저장소에 올리지 않는다.
# -----------------------------------------------------------------------------
scripts/build-intro-pdf.mjs
scripts/excload-intro.html
scripts/preview-pdf.mjs
scripts/mobile-*.mjs
scripts/*-preview-*.png
scripts/mobile-*.png
엑클로드-서비스소개서.pdf

.vercel
.env*
