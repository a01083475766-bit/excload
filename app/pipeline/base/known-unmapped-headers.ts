/**
 * 알려진 "참고/보관용" 헤더 목록
 *
 * 택배 변환에 직접 쓰이지 않는 것으로 이미 검토·확인된 원본 헤더입니다.
 * - 기준헤더/별칭에 매핑하지 않되, 미매핑(unknownHeaders) 경고 배너나 AI 자동매핑 호출
 *   대상에서는 제외합니다 (사용자에게 불필요한 "매핑 필요" 알림을 띄우지 않기 위함).
 * - 새로운 파일 양식을 검토하다가 "매핑할 필요는 없지만 경고도 띄우고 싶지 않은" 헤더를
 *   발견하면 이 목록에 추가하세요.
 *
 * ⚠️ 여기 있는 헤더라도 ALIAS_DICTIONARY/DB 별칭에 실제 매핑이 정의되면 그 매핑이 우선합니다.
 */
export const KNOWN_UNMAPPED_HEADERS: readonly string[] = [
  // ===== 스마트스토어 발주발송관리 (2026-07 검토) =====
  '배송속성',
  '풀필먼트사(주문 기준)',
  '배송방법(구매자 요청)',
  '택배사(주문 기준)',
  '수량클레임 여부',
  '결제위치',
  '상품종류',
  '반품안심케어',
  '멤버십N배송',
  '상품가격',
  '최종 상품별 총 주문금액',
  '최초 상품별 총 주문금액',
  '출고지',
  '네이버페이 주문관리 수수료',
  '매출연동 수수료',
  '정산예정금액',
  '발주확인일',
  '구독신청회차',
  '구독진행회차',
  '구독배송희망일',
  '배송태그 유형',
  '출입방법 유형',
  '출입방법 내용',
  '수령위치 유형',
  '수령위치 내용',
] as const;

const KNOWN_UNMAPPED_HEADER_SET = new Set(KNOWN_UNMAPPED_HEADERS);

/** 원본 헤더가 "알려진 참고/보관용" 헤더인지 확인합니다 (앞뒤 공백은 무시). */
export function isKnownUnmappedHeader(header: string): boolean {
  return KNOWN_UNMAPPED_HEADER_SET.has((header ?? '').trim());
}
