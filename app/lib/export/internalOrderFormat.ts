/**
 * 내부 표준 주문 엑셀 형식 정의
 *
 * 이 파일은 내부 표준 주문 데이터 형식을 정의하는 기준선(baseline)입니다.
 * NormalizationResult 기준헤더 29개 구조와 **완전히 동일한** 내부 구조를 유지합니다.
 *
 * ⚠️ 헌법 규칙:
 * - 기준헤더는 내부 처리 전용이며 UI, 미리보기, 다운로드에 직접 노출되면 안 됩니다.
 * - 이 타입은 Stage 2(Order Pipeline) / Stage 3(Merge Pipeline) 내부 전용입니다.
 *
 * 텍스트 주문 변환 / 이미지 변환 파이프라인에서 공통으로 사용하는 내부 기준 구조입니다.
 */

/**
 * 내부 표준 주문 데이터 형식 (기준헤더 29개와 1:1 대응)
 *
 * 필드 이름은 모두 "한글 기준헤더" 그대로 사용합니다.
 * - NormalizationResult 29개 필드
 * - InternalOrderFormat 29개 필드
 * - 모달/입력 매핑 구조 29개 필드
 * 를 완전히 통일합니다.
 */
export interface InternalOrderFormat {
  /** 주문번호 */
  주문번호: string;
  /** 보내는사람 */
  보내는사람: string;
  /** 보내는사람전화1 */
  보내는사람전화1: string;
  /** 보내는사람전화2 */
  보내는사람전화2: string;
  /** 보내는사람우편번호 */
  보내는사람우편번호: string;
  /** 보내는사람주소1 */
  보내는사람주소1: string;
  /** 보내는사람주소2 */
  보내는사람주소2: string;
  /** 받는사람 */
  받는사람: string;
  /** 받는사람전화1 */
  받는사람전화1: string;
  /** 받는사람전화2 */
  받는사람전화2: string;
  /** 받는사람우편번호 */
  받는사람우편번호: string;
  /** 받는사람주소1 */
  받는사람주소1: string;
  /** 받는사람주소2 */
  받는사람주소2: string;
  /** 주문자 */
  주문자: string;
  /** 주문자연락처 */
  주문자연락처: string;
  /** 주문일시 */
  주문일시: string;
  /** 결제금액 */
  결제금액: string;
  /** 상품명 */
  상품명: string;
  /** 추가상품 */
  추가상품: string;
  /** 상품옵션 */
  상품옵션: string;
  /** 상품옵션1 */
  상품옵션1: string;
  /** 수량 */
  수량: string;
  /** 배송메시지 */
  배송메시지: string;
  /** 운임구분 */
  운임구분: string;
  /** 운임 */
  운임: string;
  /** 운송장번호 */
  운송장번호: string;
  /** 창고메모 */
  창고메모: string;
  /** 내부메모 */
  내부메모: string;
  /** 출고번호 */
  출고번호: string;
}
